apiVersion: v1
kind: Service
metadata:
  name: archon-registry-service
spec:
  clusterIP: None
  selector:
    app: archon-registry
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
